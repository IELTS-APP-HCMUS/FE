<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="login_full.Components.ForgotHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:login_full.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<StackPanel Margin="90, 40, 0, 0" HorizontalAlignment="Left" Width="600">
		<StackPanel.Resources>
			<Style TargetType="TextBlock">
				<Setter Property="Foreground" Value="Black"/>
			</Style>
		</StackPanel.Resources>
		<Button Content="← Back to login" FontSize="14" HorizontalAlignment="Left" Foreground="Black"/>
		<TextBlock Text="{x:Bind Title, Mode=OneWay}" FontSize="34" FontWeight="Bold" Margin="0,40,0,10"/>
		<TextBlock Text="{x:Bind Describe, Mode=OneWay}" FontSize="14" Foreground="Gray" TextWrapping="Wrap" Margin="0,30,0,0"/>
	</StackPanel>
</UserControl>
